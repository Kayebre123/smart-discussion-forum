<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Smart Discussion Forum</title>
        @vite(['resources/css/app.css', 'resources/js/app.js'])
    </head>
    <body class="min-h-screen bg-slate-950 text-slate-100">
        <div class="mx-auto flex min-h-screen max-w-7xl flex-col px-6 py-8 lg:px-10">
            <header class="flex items-center justify-between rounded-full border border-white/10 bg-white/10 px-4 py-3 backdrop-blur">
                <div>
                    <p class="text-lg font-semibold tracking-wide">Smart Discussion Forum</p>
                    <p class="text-sm text-slate-300">A focused academic collaboration space</p>
                </div>
                <nav class="flex items-center gap-3">
                    @if (Route::has('login'))
                        @auth
                            <a href="{{ url('/dashboard') }}" class="rounded-full bg-emerald-500 px-4 py-2 text-sm font-medium text-slate-950 transition hover:bg-emerald-400">Dashboard</a>
                        @else
                            <a href="{{ route('login') }}" class="rounded-full border border-white/20 px-4 py-2 text-sm font-medium text-slate-100 transition hover:bg-white/10">Sign in</a>
                            @if (Route::has('register'))
                                <a href="{{ route('register') }}" class="rounded-full bg-sky-500 px-4 py-2 text-sm font-medium text-white transition hover:bg-sky-400">Create account</a>
                            @endif
                        @endauth
                    @endif
                </nav>
            </header>

            <main class="flex flex-1 flex-col justify-center py-16 lg:flex-row lg:items-center lg:gap-16 lg:py-24">
                <section class="max-w-2xl">
                    <span class="inline-flex rounded-full border border-sky-400/30 bg-sky-500/10 px-3 py-1 text-sm font-medium text-sky-300">
                        Built for modern learning communities
                    </span>
                    <h1 class="mt-6 text-4xl font-semibold leading-tight sm:text-5xl">
                        Bring students, lecturers, and administrators into one thoughtful discussion hub.
                    </h1>
                    <p class="mt-6 text-lg leading-8 text-slate-300">
                        Smart Discussion Forum helps your campus share announcements, host debates, track participation, and guide learners with clarity.
                    </p>
                    <div class="mt-8 flex flex-wrap gap-4">
                        <a href="{{ route('login') }}" class="rounded-full bg-white px-5 py-3 text-sm font-semibold text-slate-950 transition hover:bg-slate-200">Open the portal</a>
                        <a href="#features" class="rounded-full border border-white/20 px-5 py-3 text-sm font-semibold text-slate-100 transition hover:bg-white/10">Explore features</a>
                    </div>
                </section>

                <aside class="mt-10 w-full max-w-xl rounded-3xl border border-white/10 bg-slate-900/80 p-8 shadow-2xl shadow-slate-950/40 lg:mt-0">
                    <div class="grid gap-4 sm:grid-cols-2">
                        <div class="rounded-2xl border border-emerald-400/20 bg-emerald-500/10 p-4">
                            <p class="text-sm text-emerald-300">Live engagement</p>
                            <p class="mt-2 text-2xl font-semibold">24/7 discussions</p>
                        </div>
                        <div class="rounded-2xl border border-sky-400/20 bg-sky-500/10 p-4">
                            <p class="text-sm text-sky-300">Role-based access</p>
                            <p class="mt-2 text-2xl font-semibold">Admin, lecturer, student</p>
                        </div>
                        <div class="rounded-2xl border border-violet-400/20 bg-violet-500/10 p-4 sm:col-span-2">
                            <p class="text-sm text-violet-300">Campus-ready experience</p>
                            <p class="mt-2 text-xl font-semibold">Structured spaces, clear moderation, and streamlined dashboards.</p>
                        </div>
                    </div>
                </aside>
            </main>

            <section id="features" class="grid gap-6 border-t border-white/10 py-10 md:grid-cols-3">
                <div class="rounded-2xl border border-white/10 bg-white/5 p-6">
                    <h2 class="text-lg font-semibold">Guided conversations</h2>
                    <p class="mt-3 text-sm leading-7 text-slate-300">Create focused topic threads that support teaching, peer review, and academic exchange.</p>
                </div>
                <div class="rounded-2xl border border-white/10 bg-white/5 p-6">
                    <h2 class="text-lg font-semibold">Clear moderation</h2>
                    <p class="mt-3 text-sm leading-7 text-slate-300">Administrators and lecturers can oversee participation, warnings, and member activity with confidence.</p>
                </div>
                <div class="rounded-2xl border border-white/10 bg-white/5 p-6">
                    <h2 class="text-lg font-semibold">Personalized dashboards</h2>
                    <p class="mt-3 text-sm leading-7 text-slate-300">Students receive learning-focused views while lecturers manage grading and quizzes in one place.</p>
                </div>
            </section>
        </div>
    </body>
</html>
